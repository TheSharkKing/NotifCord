def announce_stream(user, platform):
    message = f"{user} has gone live on {platform}!"
    print(message)
    # You can add more functionality here, such as sending the message to a chat or logging it

# Example usage
announce_stream("StreamerName", "Twitch")